/*
    scanner.cpp

    parses a single lexeme and returns its token ID, string instance,
    and line number
*/

#include "scanner.h"
#include "token.h"
#include <iostream>
#include <fstream>

token scanner(std::ifstream& file) {
    /* opens file provided as argument, parses*/

    token newToken;

    return newToken;
}